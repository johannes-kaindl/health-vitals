# Slice 3b — i18n (DE/EN) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das Health-Vitals-Dashboard zweisprachig (DE/EN) machen, Sprache automatisch aus Obsidians UI-Locale, kein Setting.

**Architecture:** i18n-Engine aus `obsidian-kit` verbatim vendoren (`pickLang`/`setLang`/`getLang`/`defineStrings`/`t`, modul-global, EN-Fallback). Plugin-eigene Strings in `src/i18n/strings.ts`. Der reine Kern bleibt obsidian-frei und nutzt `t()`/`getLang()` modul-global (kein Parameter-Threading). Kern der Arbeit: `Category` von deutschem Anzeige-Literal zu neutralem `CategoryKey` entkoppeln (Identität vs. Anzeige).

**Tech Stack:** TypeScript, Vitest, esbuild, Obsidian API (`getLanguage()`).

## Global Constraints

- **minAppVersion 1.8.0** → `getLanguage()` ist garantiert verfügbar; Zugriff dennoch defensiv `try { getLanguage() } catch { null }` (Muster wie epub-exporter/image-to-markdown).
- **Reiner Kern (`src/core/`, `src/vendor/`) ohne obsidian-/DOM-Import** (PROF-OBS-03/04). `getLanguage()` nur im obsidian-Layer (`src/main.ts`).
- **EN ist kanonisch + universeller Fallback** (`t()` fällt `currentLang → en → key`).
- **`src/vendor/kit/i18n.ts` ist eine verbatim-Kopie** von `obsidian-kit/src/pure/i18n.ts` — nicht abändern.
- **CSS-Prefix `ah-`, ViewType `apple-health-dashboard`, Cache-Schema** bleiben unverändert.
- **Sprachdetektion:** `pickLang(getLanguage())` (nicht `detectLang`/localStorage).
- Alle Tests laufen über `npm test` (`vitest run`); Lint über `npm run lint`; Typecheck `npm run typecheck`.

---

## Task 1: i18n-Engine vendoren + Strings-Modul + globales Test-Setup

**Files:**
- Create: `src/vendor/kit/i18n.ts` (verbatim-Kopie)
- Create: `src/i18n/strings.ts`
- Create: `tests/setup.ts`
- Modify: `vitest.config.ts` (setupFiles)
- Test: `tests/i18n/strings.test.ts`

**Interfaces:**
- Consumes: nichts.
- Produces:
  - `src/vendor/kit/i18n.ts`: `type Lang = "en" | "de"`, `pickLang(raw?: string | null): Lang`, `setLang(l: Lang): void`, `getLang(): Lang`, `defineStrings(dicts: Record<Lang, Record<string,string>>): void`, `t(key: string, ...args: (string|number)[]): string`.
  - `src/i18n/strings.ts`: `registerI18n(): void` (ruft `defineStrings({ en: EN, de: DE })`), `localeTag(): string` (`getLang()==="de" ? "de-DE" : "en-US"`), plus re-exports `setLang`, `getLang`, `pickLang`, `t`.

- [ ] **Step 1: Vendored Engine anlegen** (verbatim aus `../obsidian-kit/src/pure/i18n.ts`)

Create `src/vendor/kit/i18n.ts`:

```ts
// i18n-Engine (EN/DE) — pure (kein obsidian-/DOM-Import, PROF-OBS-03/04).
// VERBATIM-Kopie aus obsidian-kit/src/pure/i18n.ts (PROF-OBS-07). Nicht abändern —
// plugin-eigene Strings werden via defineStrings() injiziert (src/i18n/strings.ts).
export type Lang = "en" | "de";
type Dict = Record<string, string>;

let currentLang: Lang = "en";
let strings: Record<Lang, Dict> = { en: {}, de: {} };

/** Wählt die Sprache aus einem rohen Locale-String (z.B. von obsidian.getLanguage()). */
export function pickLang(raw?: string | null): Lang {
  return raw && raw.toLowerCase().startsWith("de") ? "de" : "en";
}

export function setLang(lang: Lang): void { currentLang = lang; }
export function getLang(): Lang { return currentLang; }

/** Registriert die Plugin-eigenen UI-Strings. Einmalig vor dem ersten t() (typisch im onload).
 *  EN ist kanonisch und universeller Fallback. */
export function defineStrings(dicts: Record<Lang, Dict>): void { strings = dicts; }

/** Übersetzt key in der aktuellen Sprache; Fallback currentLang → en → key. {0},{1}… aus args. */
export function t(key: string, ...args: (string | number)[]): string {
  const raw = strings[currentLang][key] ?? strings.en[key] ?? key;
  return raw.replace(/\{(\d+)\}/g, (_m, i) => {
    const v = args[Number(i)];
    return v === undefined ? `{${i}}` : String(v);
  });
}
```

- [ ] **Step 2: Strings-Modul schreiben**

Create `src/i18n/strings.ts` (vollständige EN/DE-Dicts — Key-Parität ist Pflicht):

```ts
import { defineStrings, getLang, setLang, pickLang, t } from "../vendor/kit/i18n";
export { setLang, getLang, pickLang, t };

export const EN: Record<string, string> = {
  // commands / chrome
  "cmd.openDashboard": "Open dashboard",
  "view.title": "Health Vitals",
  "ribbon.tooltip": "Health Vitals dashboard",
  "tab.overview": "Overview",
  "tab.detail": "Detail",
  "tab.workouts": "Workouts",
  // categories
  "category.activity": "Activity",
  "category.heart": "Heart",
  "category.body": "Body",
  "category.sleep": "Sleep",
  "category.nutrition": "Nutrition",
  "category.other": "Other",
  // stat labels
  "stat.avg": "Avg",
  "stat.min": "Min",
  "stat.max": "Max",
  "stat.last": "Last",
  "stat.avgPerDay": "Avg/day",
  "stat.maxDay": "Peak day",
  "stat.total": "Total",
  // ranges
  "range.1M": "1M",
  "range.3M": "3M",
  "range.1Y": "1Y",
  "range.all": "All",
  // overview
  "overview.favorites": "★ Favorites",
  "overview.emptyMetrics": "No metrics in the import.",
  "a11y.openMetric": "Open {0}",
  "a11y.addFavorite": "Add to favorites",
  "a11y.removeFavorite": "Remove from favorites",
  "log.favSaveFailed": "Health Vitals: saving favorite failed",
  // detail
  "detail.pickMetric": "Pick a metric in the overview.",
  "detail.noData": "No data in this range.",
  // import
  "import.running.title": "Import running",
  "import.phase.unzipping": "Unpacking export…",
  "import.phase.parsing": "Reading data…",
  "import.phase.writing": "Saving result…",
  "import.records": "{0} records",
  "import.cancel": "Cancel",
  "import.failed.title": "Import failed",
  "import.retry": "Try again",
  "import.aborted.title": "Import canceled",
  "import.aborted.body": "No data was saved.",
  "import.idle.title": "No data yet",
  "import.idle.body": "Export your data in the Health app (Profile → Export All Health Data) and pick the resulting file here.",
  "import.choose": "Choose export",
  // workouts
  "workouts.emptyExport": "No workouts in the export.",
  "workouts.perMonth": "Workouts per month",
  "workouts.recent": "Recent workouts",
  // errors
  "error.exportNotFound": "Export.xml not found in the zip",
  // metrics (key = full HK identifier)
  "metric.HKQuantityTypeIdentifierStepCount": "Steps",
  "metric.HKQuantityTypeIdentifierDistanceWalkingRunning": "Walking Distance",
  "metric.HKQuantityTypeIdentifierDistanceCycling": "Cycling Distance",
  "metric.HKQuantityTypeIdentifierFlightsClimbed": "Flights Climbed",
  "metric.HKQuantityTypeIdentifierActiveEnergyBurned": "Active Energy",
  "metric.HKQuantityTypeIdentifierBasalEnergyBurned": "Resting Energy",
  "metric.HKQuantityTypeIdentifierAppleExerciseTime": "Exercise Minutes",
  "metric.HKQuantityTypeIdentifierAppleStandTime": "Stand Minutes",
  "metric.HKQuantityTypeIdentifierHeartRate": "Heart Rate",
  "metric.HKQuantityTypeIdentifierRestingHeartRate": "Resting Heart Rate",
  "metric.HKQuantityTypeIdentifierWalkingHeartRateAverage": "Walking HR Avg",
  "metric.HKQuantityTypeIdentifierHeartRateVariabilitySDNN": "HRV",
  "metric.HKQuantityTypeIdentifierOxygenSaturation": "Oxygen Saturation",
  "metric.HKQuantityTypeIdentifierBodyMass": "Weight",
  "metric.HKQuantityTypeIdentifierBodyMassIndex": "BMI",
  "metric.HKQuantityTypeIdentifierBodyFatPercentage": "Body Fat",
  "metric.HKQuantityTypeIdentifierHeight": "Height",
  "metric.HKQuantityTypeIdentifierBodyTemperature": "Body Temperature",
  "metric.HKCategoryTypeIdentifierSleepAnalysis": "Sleep",
  "metric.HKCategoryTypeIdentifierMindfulSession": "Mindfulness",
  "metric.HKQuantityTypeIdentifierDietaryWater": "Water",
  "metric.HKQuantityTypeIdentifierDietaryEnergyConsumed": "Calories",
  "metric.HKQuantityTypeIdentifierDietaryProtein": "Protein",
  "metric.HKQuantityTypeIdentifierDietaryCarbohydrates": "Carbohydrates",
  "metric.HKQuantityTypeIdentifierDietaryFatTotal": "Fat",
  // workouts (key = full HK identifier)
  "workout.HKWorkoutActivityTypeCardioDance": "Cardio Dance",
  "workout.HKWorkoutActivityTypeCoreTraining": "Core Training",
  "workout.HKWorkoutActivityTypeCrossTraining": "Cross Training",
  "workout.HKWorkoutActivityTypeCycling": "Cycling",
  "workout.HKWorkoutActivityTypeElliptical": "Elliptical",
  "workout.HKWorkoutActivityTypeFitnessGaming": "Fitness Gaming",
  "workout.HKWorkoutActivityTypeFunctionalStrengthTraining": "Functional Strength Training",
  "workout.HKWorkoutActivityTypeHiking": "Hiking",
  "workout.HKWorkoutActivityTypeMindAndBody": "Mind & Body",
  "workout.HKWorkoutActivityTypeOther": "Other",
  "workout.HKWorkoutActivityTypePreparationAndRecovery": "Prep & Recovery",
  "workout.HKWorkoutActivityTypeRunning": "Running",
  "workout.HKWorkoutActivityTypeSwimming": "Swimming",
  "workout.HKWorkoutActivityTypeTableTennis": "Table Tennis",
  "workout.HKWorkoutActivityTypeTraditionalStrengthTraining": "Strength Training",
  "workout.HKWorkoutActivityTypeUnderwaterDiving": "Diving",
  "workout.HKWorkoutActivityTypeWalking": "Walking",
  "workout.HKWorkoutActivityTypeYoga": "Yoga",
};

export const DE: Record<string, string> = {
  "cmd.openDashboard": "Dashboard öffnen",
  "view.title": "Health Vitals",
  "ribbon.tooltip": "Health Vitals Dashboard",
  "tab.overview": "Übersicht",
  "tab.detail": "Detail",
  "tab.workouts": "Workouts",
  "category.activity": "Aktivität",
  "category.heart": "Herz",
  "category.body": "Körper",
  "category.sleep": "Schlaf",
  "category.nutrition": "Ernährung",
  "category.other": "Sonstige",
  "stat.avg": "Ø",
  "stat.min": "Min",
  "stat.max": "Max",
  "stat.last": "Zuletzt",
  "stat.avgPerDay": "Ø/Tag",
  "stat.maxDay": "Max-Tag",
  "stat.total": "Summe",
  "range.1M": "1M",
  "range.3M": "3M",
  "range.1Y": "1J",
  "range.all": "Alles",
  "overview.favorites": "★ Favoriten",
  "overview.emptyMetrics": "Keine Metriken im Import.",
  "a11y.openMetric": "{0} öffnen",
  "a11y.addFavorite": "Zu Favoriten",
  "a11y.removeFavorite": "Aus Favoriten entfernen",
  "log.favSaveFailed": "Health Vitals: Favorit speichern fehlgeschlagen",
  "detail.pickMetric": "Wähle in der Übersicht eine Metrik aus.",
  "detail.noData": "Keine Daten in diesem Zeitraum.",
  "import.running.title": "Import läuft",
  "import.phase.unzipping": "Export wird entpackt …",
  "import.phase.parsing": "Daten werden gelesen …",
  "import.phase.writing": "Ergebnis wird gespeichert …",
  "import.records": "{0} Datensätze",
  "import.cancel": "Abbrechen",
  "import.failed.title": "Import fehlgeschlagen",
  "import.retry": "Erneut versuchen",
  "import.aborted.title": "Import abgebrochen",
  "import.aborted.body": "Es wurden keine Daten gespeichert.",
  "import.idle.title": "Noch keine Daten",
  "import.idle.body": "Exportiere deine Daten in der Health-App (Profil → Alle Gesundheitsdaten exportieren) und wähle hier die entstandene Datei aus.",
  "import.choose": "Export auswählen",
  "workouts.emptyExport": "Keine Workouts im Export.",
  "workouts.perMonth": "Workouts pro Monat",
  "workouts.recent": "Letzte Workouts",
  "error.exportNotFound": "Export.xml nicht im Zip gefunden",
  "metric.HKQuantityTypeIdentifierStepCount": "Schritte",
  "metric.HKQuantityTypeIdentifierDistanceWalkingRunning": "Gehstrecke",
  "metric.HKQuantityTypeIdentifierDistanceCycling": "Radstrecke",
  "metric.HKQuantityTypeIdentifierFlightsClimbed": "Etagen",
  "metric.HKQuantityTypeIdentifierActiveEnergyBurned": "Aktive Energie",
  "metric.HKQuantityTypeIdentifierBasalEnergyBurned": "Ruheenergie",
  "metric.HKQuantityTypeIdentifierAppleExerciseTime": "Bewegungsminuten",
  "metric.HKQuantityTypeIdentifierAppleStandTime": "Stehminuten",
  "metric.HKQuantityTypeIdentifierHeartRate": "Puls",
  "metric.HKQuantityTypeIdentifierRestingHeartRate": "Ruhepuls",
  "metric.HKQuantityTypeIdentifierWalkingHeartRateAverage": "Geh-Puls Ø",
  "metric.HKQuantityTypeIdentifierHeartRateVariabilitySDNN": "HRV",
  "metric.HKQuantityTypeIdentifierOxygenSaturation": "Sauerstoffsättigung",
  "metric.HKQuantityTypeIdentifierBodyMass": "Gewicht",
  "metric.HKQuantityTypeIdentifierBodyMassIndex": "BMI",
  "metric.HKQuantityTypeIdentifierBodyFatPercentage": "Körperfett",
  "metric.HKQuantityTypeIdentifierHeight": "Größe",
  "metric.HKQuantityTypeIdentifierBodyTemperature": "Körpertemperatur",
  "metric.HKCategoryTypeIdentifierSleepAnalysis": "Schlaf",
  "metric.HKCategoryTypeIdentifierMindfulSession": "Achtsamkeit",
  "metric.HKQuantityTypeIdentifierDietaryWater": "Wasser",
  "metric.HKQuantityTypeIdentifierDietaryEnergyConsumed": "Kalorien",
  "metric.HKQuantityTypeIdentifierDietaryProtein": "Protein",
  "metric.HKQuantityTypeIdentifierDietaryCarbohydrates": "Kohlenhydrate",
  "metric.HKQuantityTypeIdentifierDietaryFatTotal": "Fett",
  "workout.HKWorkoutActivityTypeCardioDance": "Cardio-Dance",
  "workout.HKWorkoutActivityTypeCoreTraining": "Core-Training",
  "workout.HKWorkoutActivityTypeCrossTraining": "Cross-Training",
  "workout.HKWorkoutActivityTypeCycling": "Radfahren",
  "workout.HKWorkoutActivityTypeElliptical": "Crosstrainer",
  "workout.HKWorkoutActivityTypeFitnessGaming": "Fitness-Gaming",
  "workout.HKWorkoutActivityTypeFunctionalStrengthTraining": "Funktionelles Krafttraining",
  "workout.HKWorkoutActivityTypeHiking": "Wandern",
  "workout.HKWorkoutActivityTypeMindAndBody": "Körper & Geist",
  "workout.HKWorkoutActivityTypeOther": "Sonstiges",
  "workout.HKWorkoutActivityTypePreparationAndRecovery": "Aufwärmen & Erholung",
  "workout.HKWorkoutActivityTypeRunning": "Laufen",
  "workout.HKWorkoutActivityTypeSwimming": "Schwimmen",
  "workout.HKWorkoutActivityTypeTableTennis": "Tischtennis",
  "workout.HKWorkoutActivityTypeTraditionalStrengthTraining": "Krafttraining",
  "workout.HKWorkoutActivityTypeUnderwaterDiving": "Tauchen",
  "workout.HKWorkoutActivityTypeWalking": "Gehen",
  "workout.HKWorkoutActivityTypeYoga": "Yoga",
};

/** Einmalig im onload (obsidian-Layer) und im Test-Setup aufrufen — registriert die Dicts. */
export function registerI18n(): void {
  defineStrings({ en: EN, de: DE });
}

/** BCP-47-Tag für toLocaleString/localeCompare, aus der aktiven Sprache. */
export function localeTag(): string {
  return getLang() === "de" ? "de-DE" : "en-US";
}
```

- [ ] **Step 3: Globales Test-Setup schreiben**

Create `tests/setup.ts` — setzt vor JEDEM Test die i18n auf Deutsch (Default), damit bestehende deutsche Assertions grün bleiben. EN-Tests rufen `setLang("en")` explizit im Test-Body:

```ts
import { beforeEach } from "vitest";
import { registerI18n, setLang } from "../src/i18n/strings";

beforeEach(() => {
  registerI18n();
  setLang("de");
});
```

- [ ] **Step 4: vitest.config um setupFiles ergänzen**

Modify `vitest.config.ts` — im `test`-Block `setupFiles` hinzufügen:

```ts
  test: {
    environment: "node",
    globals: true,
    setupFiles: ["./tests/setup.ts"],
  },
```

- [ ] **Step 5: Test schreiben (Key-Parität, localeTag, t-Substitution)**

Create `tests/i18n/strings.test.ts`:

```ts
import { EN, DE, registerI18n, localeTag } from "../../src/i18n/strings";
import { setLang, t } from "../../src/vendor/kit/i18n";

describe("strings", () => {
  it("EN und DE haben identische Keysets (Parität)", () => {
    const en = Object.keys(EN).sort();
    const de = Object.keys(DE).sort();
    expect(de).toEqual(en);
  });

  it("registerI18n + setLang liefert die richtige Sprache, EN als Fallback", () => {
    registerI18n();
    setLang("de");
    expect(t("tab.overview")).toBe("Übersicht");
    setLang("en");
    expect(t("tab.overview")).toBe("Overview");
    expect(t("does.not.exist")).toBe("does.not.exist"); // Key-Fallback
  });

  it("t substituiert Positionsargumente", () => {
    registerI18n();
    setLang("en");
    expect(t("a11y.openMetric", "Steps")).toBe("Open Steps");
    expect(t("import.records", 1234)).toBe("1234 records");
  });

  it("localeTag folgt der aktiven Sprache", () => {
    setLang("de");
    expect(localeTag()).toBe("de-DE");
    setLang("en");
    expect(localeTag()).toBe("en-US");
  });
});
```

- [ ] **Step 6: Tests laufen lassen**

Run: `npm test -- tests/i18n/strings.test.ts`
Expected: PASS (4 Tests). Danach `npm run typecheck` → keine Fehler.

- [ ] **Step 7: Commit**

```bash
git add src/vendor/kit/i18n.ts src/i18n/strings.ts tests/setup.ts tests/i18n/strings.test.ts vitest.config.ts
git commit -m "feat(i18n): vendor kit i18n engine + plugin strings + global test setup"
```

---

## Task 2: `Category` → `CategoryKey` entkoppeln (metric-catalog + view-model + overview)

Der Kern der Arbeit: die deutschen `Category`-Literale sind heute gleichzeitig Identität (Grouping, `CATEGORY_ORDER`, `<details>`-`expanded`-Set) und Anzeige. Diese Task spaltet beides und übersetzt Metrik-Namen + Stat-Labels + Overview-Chrome. Alle `Category`-Consumer liegen in diesen drei Dateien, daher eine Task.

**Files:**
- Modify: `src/core/metric-catalog.ts`
- Modify: `src/core/view-model.ts`
- Modify: `src/obsidian/tabs/overview.ts`
- Test: `tests/core/metric-catalog.test.ts`, `tests/core/view-model.test.ts`, `tests/obsidian/overview.test.ts`

**Interfaces:**
- Consumes: `t` (`src/vendor/kit/i18n`), `localeTag` (`src/i18n/strings`).
- Produces:
  - `metric-catalog.ts`: `type Category = "activity" | "heart" | "body" | "sleep" | "nutrition" | "other"`; `describeMetric(id: string, policy: Policy): MetricInfo` unverändert in Signatur, `name` nun via `t("metric."+id)`, `category` als Key.
  - `view-model.ts`: `OverviewVM.sections: Array<{ category: Category; categoryLabel: string; tiles: TileVM[] }>` (Feld `category` = Key, neu `categoryLabel` = Anzeige). `TileVM.category: Category` (Key). `CATEGORY_ORDER: Category[]` = Keys.

- [ ] **Step 1: metric-catalog.test.ts auf Keys + beide Sprachen umstellen (failing)**

Replace `tests/core/metric-catalog.test.ts`:

```ts
import { describeMetric } from "../../src/core/metric-catalog";
import { setLang } from "../../src/vendor/kit/i18n";

describe("metric-catalog", () => {
  it("kennt kuratierte Identifier: neutrale Kategorie-Keys, Name je Sprache", () => {
    setLang("de");
    expect(describeMetric("HKQuantityTypeIdentifierStepCount", "sum"))
      .toEqual({ name: "Schritte", category: "activity", chartKind: "bar" });
    expect(describeMetric("HKQuantityTypeIdentifierBodyMass", "measure"))
      .toEqual({ name: "Gewicht", category: "body", chartKind: "line" });
    setLang("en");
    expect(describeMetric("HKQuantityTypeIdentifierStepCount", "sum"))
      .toEqual({ name: "Steps", category: "activity", chartKind: "bar" });
    expect(describeMetric("HKQuantityTypeIdentifierBodyMass", "measure"))
      .toEqual({ name: "Weight", category: "body", chartKind: "line" });
  });

  it("leitet chartKind aus der Policy ab, wenn der Katalog keinen Override hat", () => {
    expect(describeMetric("HKQuantityTypeIdentifierHeartRate", "measure").chartKind).toBe("line");
    expect(describeMetric("HKCategoryTypeIdentifierMindfulSession", "duration").chartKind).toBe("bar");
  });

  it("Fallback für Unbekannte: Prefix strippen, CamelCase splitten, Kategorie other (sprachneutral)", () => {
    setLang("de");
    expect(describeMetric("HKQuantityTypeIdentifierDietaryZinc", "measure"))
      .toEqual({ name: "Dietary Zinc", category: "other", chartKind: "line" });
    setLang("en");
    expect(describeMetric("HKCategoryTypeIdentifierFooBar", "duration"))
      .toEqual({ name: "Foo Bar", category: "other", chartKind: "bar" });
  });
});
```

- [ ] **Step 2: Test laufen — muss fehlschlagen**

Run: `npm test -- tests/core/metric-catalog.test.ts`
Expected: FAIL (category noch "Aktivität"/"Körper"/"Sonstige", `t` noch nicht verdrahtet).

- [ ] **Step 3: metric-catalog.ts umstellen**

Replace `src/core/metric-catalog.ts`:

```ts
import type { Policy } from "./aggregation-policy";
import { t } from "../vendor/kit/i18n";

export type Category = "activity" | "heart" | "body" | "sleep" | "nutrition" | "other";
export type ChartKind = "line" | "bar";
export interface MetricInfo { name: string; category: Category; chartKind: ChartKind; }

interface CatalogEntry { category: Category; chartKind?: ChartKind; }

// Kuratierter Katalog der häufigen Identifier: nur Struktur (Kategorie/ChartKind).
// Die Anzeigenamen liegen sprachabhängig in src/i18n/strings.ts unter "metric.<id>".
const CATALOG: Record<string, CatalogEntry> = {
  HKQuantityTypeIdentifierStepCount: { category: "activity" },
  HKQuantityTypeIdentifierDistanceWalkingRunning: { category: "activity" },
  HKQuantityTypeIdentifierDistanceCycling: { category: "activity" },
  HKQuantityTypeIdentifierFlightsClimbed: { category: "activity" },
  HKQuantityTypeIdentifierActiveEnergyBurned: { category: "activity" },
  HKQuantityTypeIdentifierBasalEnergyBurned: { category: "activity" },
  HKQuantityTypeIdentifierAppleExerciseTime: { category: "activity" },
  HKQuantityTypeIdentifierAppleStandTime: { category: "activity" },
  HKQuantityTypeIdentifierHeartRate: { category: "heart" },
  HKQuantityTypeIdentifierRestingHeartRate: { category: "heart" },
  HKQuantityTypeIdentifierWalkingHeartRateAverage: { category: "heart" },
  HKQuantityTypeIdentifierHeartRateVariabilitySDNN: { category: "heart" },
  HKQuantityTypeIdentifierOxygenSaturation: { category: "heart" },
  HKQuantityTypeIdentifierBodyMass: { category: "body" },
  HKQuantityTypeIdentifierBodyMassIndex: { category: "body" },
  HKQuantityTypeIdentifierBodyFatPercentage: { category: "body" },
  HKQuantityTypeIdentifierHeight: { category: "body" },
  HKQuantityTypeIdentifierBodyTemperature: { category: "body" },
  HKCategoryTypeIdentifierSleepAnalysis: { category: "sleep" },
  HKCategoryTypeIdentifierMindfulSession: { category: "sleep" },
  HKQuantityTypeIdentifierDietaryWater: { category: "nutrition" },
  HKQuantityTypeIdentifierDietaryEnergyConsumed: { category: "nutrition" },
  HKQuantityTypeIdentifierDietaryProtein: { category: "nutrition" },
  HKQuantityTypeIdentifierDietaryCarbohydrates: { category: "nutrition" },
  HKQuantityTypeIdentifierDietaryFatTotal: { category: "nutrition" },
};

function chartFromPolicy(policy: Policy): ChartKind {
  return policy === "measure" ? "line" : "bar";
}

function fallbackName(id: string): string {
  const stripped = id
    .replace(/^HKQuantityTypeIdentifier/, "")
    .replace(/^HKCategoryTypeIdentifier/, "")
    .replace(/^HKDataTypeIdentifier/, "");
  // CamelCase → Wörter: "DietaryZinc" → "Dietary Zinc"
  return stripped.replace(/([a-z0-9])([A-Z])/g, "$1 $2").trim() || id;
}

export function describeMetric(id: string, policy: Policy): MetricInfo {
  const entry = CATALOG[id];
  if (entry) {
    return { name: t("metric." + id), category: entry.category, chartKind: entry.chartKind ?? chartFromPolicy(policy) };
  }
  return { name: fallbackName(id), category: "other", chartKind: chartFromPolicy(policy) };
}
```

- [ ] **Step 4: metric-catalog-Test grün**

Run: `npm test -- tests/core/metric-catalog.test.ts`
Expected: PASS. (`npm run typecheck` schlägt jetzt in view-model.ts/overview.ts fehl — das ist erwartet, wird in Step 5–8 behoben.)

- [ ] **Step 5: view-model.ts umstellen**

Replace `src/core/view-model.ts` (Änderungen: `CATEGORY_ORDER` = Keys; Sektion mit `categoryLabel = t("category."+key)`; `localeCompare(localeTag())`; Stat-Labels via `t("stat.*")`):

```ts
import type { HealthCache } from "./types";
import { describeMetric, type Category } from "./metric-catalog";
import { resolveRange, rollupDaily, type RangeKey } from "./rollup";
import { buildChartGeometry, type ChartDims, type ChartGeometry } from "./chart-geometry";
import { computeStats } from "./series-stats";
import { formatValue } from "./format";
import { t } from "../vendor/kit/i18n";
import { localeTag } from "../i18n/strings";

export interface TileVM { id: string; name: string; category: Category; valueText: string; spark: ChartGeometry; }
export interface OverviewVM {
  favorites: TileVM[];
  sections: Array<{ category: Category; categoryLabel: string; tiles: TileVM[] }>;
}
export interface StatRow { label: string; value: string; }
export interface DetailVM {
  id: string; name: string; unit: string; empty: boolean;
  rangeLabel: string; chart: ChartGeometry; stats: StatRow[];
}

export const CATEGORY_ORDER: Category[] = ["activity", "heart", "body", "sleep", "nutrition", "other"];

function tileFor(cache: HealthCache, id: string, sparkDims: ChartDims): TileVM {
  const series = cache.metrics[id];
  const info = describeMetric(id, series.policy);
  const range = cache.dateRange ?? { from: "0000-01-01", to: "9999-12-31" };
  const r = resolveRange("all", range);
  const points = rollupDaily(series.daily, series.policy, r);
  const stats = computeStats(series.daily, series.policy, r);
  const headline = series.policy === "measure" ? stats.avg ?? 0 : stats.avgPerDay ?? 0;
  return {
    id, name: info.name, category: info.category,
    valueText: formatValue(headline, series.unit),
    spark: buildChartGeometry(points, info.chartKind, sparkDims),
  };
}

export function buildOverviewVM(cache: HealthCache, favorites: string[], sparkDims: ChartDims): OverviewVM {
  const favSet = new Set(favorites);
  const ids = Object.keys(cache.metrics);
  const favTiles = ids.filter((id) => favSet.has(id)).map((id) => tileFor(cache, id, sparkDims));

  const byCat = new Map<Category, TileVM[]>();
  for (const id of ids) {
    if (favSet.has(id)) continue;
    const tile = tileFor(cache, id, sparkDims);
    const list = byCat.get(tile.category) ?? [];
    list.push(tile);
    byCat.set(tile.category, list);
  }
  const sections = CATEGORY_ORDER
    .filter((c) => byCat.has(c))
    .map((category) => ({
      category,
      categoryLabel: t("category." + category),
      tiles: (byCat.get(category) as TileVM[]).sort((a, b) => a.name.localeCompare(b.name, localeTag())),
    }));
  return { favorites: favTiles, sections };
}

export function buildDetailVM(cache: HealthCache, metricId: string, range: RangeKey, dims: ChartDims): DetailVM {
  const series = cache.metrics[metricId];
  if (!series || !cache.dateRange) {
    return { id: metricId, name: metricId, unit: "", empty: true, rangeLabel: "", chart: buildChartGeometry([], "line", dims), stats: [] };
  }
  const info = describeMetric(metricId, series.policy);
  const r = resolveRange(range, cache.dateRange);
  const points = rollupDaily(series.daily, series.policy, r);
  const chart = buildChartGeometry(points, info.chartKind, dims);
  const s = computeStats(series.daily, series.policy, r);
  const stats: StatRow[] = series.policy === "measure"
    ? [
        { label: t("stat.avg"), value: s.avg !== undefined ? formatValue(s.avg, series.unit) : "—" },
        { label: t("stat.min"), value: s.min !== undefined ? formatValue(s.min, series.unit) : "—" },
        { label: t("stat.max"), value: s.max !== undefined ? formatValue(s.max, series.unit) : "—" },
        { label: t("stat.last"), value: s.last !== undefined ? formatValue(s.last, series.unit) : "—" },
      ]
    : [
        { label: t("stat.avgPerDay"), value: s.avgPerDay !== undefined ? formatValue(s.avgPerDay, series.unit) : "—" },
        { label: t("stat.maxDay"), value: s.maxDay !== undefined ? formatValue(s.maxDay, series.unit) : "—" },
        { label: t("stat.total"), value: s.total !== undefined ? formatValue(s.total, series.unit) : "—" },
      ];
  const rangeLabel = points.length ? `${points[0].key} – ${points[points.length - 1].key}` : "";
  return { id: metricId, name: info.name, unit: series.unit, empty: points.length === 0, rangeLabel, chart, stats };
}
```

- [ ] **Step 6: view-model.test.ts anpassen (nur Setup, deutsche Assertions bleiben via globalem setLang("de"))**

Modify `tests/core/view-model.test.ts` — die vorhandenen Namens-Assertions (`"Gewicht"`, `"Schritte"`, `"Summe"`) bleiben unter dem globalen `setLang("de")` grün. **Keine Änderung nötig**, außer einer neuen Absicherung der Entkopplung. Füge am Ende des `describe("buildOverviewVM", …)` einen Test an:

```ts
  it("Sektionen tragen neutrale Kategorie-Keys + lokalisiertes Label", () => {
    const vm = buildOverviewVM(cache(), [], { width: 60, height: 24, padding: 2 });
    const activity = vm.sections.find((s) => s.category === "activity");
    expect(activity).toBeDefined();
    expect(activity!.categoryLabel).toBe("Aktivität"); // de aus globalem Setup
    const body = vm.sections.find((s) => s.category === "body");
    expect(body!.categoryLabel).toBe("Körper");
  });
```

- [ ] **Step 7: overview.ts umstellen (Anzeige via categoryLabel, Set-Key via category, Chrome via t())**

Replace `src/obsidian/tabs/overview.ts`:

```ts
import { setIcon } from "obsidian";
import type { HealthCache } from "../../core/types";
import { buildOverviewVM, type TileVM } from "../../core/view-model";
import { renderChart } from "../chart-render";
import type { DashboardView } from "../dashboard-view";
import { t } from "../../vendor/kit/i18n";

const SPARK_DIMS = { width: 120, height: 36, padding: 2 };

export function renderOverview(el: HTMLElement, cache: HealthCache, view: DashboardView): void {
  const favorites = view.host.getFavorites();
  const vm = buildOverviewVM(cache, favorites, SPARK_DIMS);

  if (vm.favorites.length) {
    const favSection = el.createDiv({ cls: "ah-fav-section" });
    favSection.createEl("h3", { text: t("overview.favorites") });
    const grid = favSection.createDiv({ cls: "ah-tile-grid" });
    for (const tile of vm.favorites) renderTile(grid, tile, cache, view, true);
  }

  // Aufklapp-Zustand aus dem View: überlebt Re-Render (z.B. Favoriten-Toggle).
  // Schlüssel ist der sprachneutrale Kategorie-Key (section.category), nicht das Label.
  const expanded = view.expandedCategories();
  view.seedExpandedOnce(() => { if (vm.sections.length > 0) expanded.add(vm.sections[0].category); });

  for (const section of vm.sections) {
    const details = el.createEl("details", { cls: "ah-cat" });
    if (expanded.has(section.category)) details.setAttribute("open", "");
    const summary = details.createEl("summary", { text: `${section.categoryLabel} (${section.tiles.length})` });
    summary.addClass("ah-cat-summary");
    details.addEventListener("toggle", () => {
      if (details.open) expanded.add(section.category);
      else expanded.delete(section.category);
    });
    const grid = details.createDiv({ cls: "ah-tile-grid" });
    for (const tile of section.tiles) renderTile(grid, tile, cache, view, false);
  }

  if (vm.favorites.length === 0 && vm.sections.length === 0) {
    el.createDiv({ cls: "ah-detail-hint", text: t("overview.emptyMetrics") });
  }
}

function renderTile(grid: HTMLElement, tile: TileVM, _cache: HealthCache, view: DashboardView, isFav: boolean): void {
  const el = grid.createDiv({ cls: "ah-tile" });
  el.setAttribute("role", "button");
  el.setAttribute("aria-label", t("a11y.openMetric", tile.name));
  el.addEventListener("click", () => view.openDetail(tile.id));

  const head = el.createDiv({ cls: "ah-tile-head" });
  head.createSpan({ cls: "ah-tile-name", text: tile.name });
  const star = head.createSpan({ cls: "ah-tile-star" });
  setIcon(star, isFav ? "star" : "star-off");
  star.setAttribute("aria-label", isFav ? t("a11y.removeFavorite") : t("a11y.addFavorite"));
  star.addEventListener("click", (ev: MouseEvent) => {
    ev.stopPropagation();
    void view.host.toggleFavorite(tile.id)
      .then(() => view.refreshOverview())
      .catch((e) => console.error(t("log.favSaveFailed"), e));
  });

  el.createDiv({ cls: "ah-tile-value", text: tile.valueText });
  const chartBox = el.createDiv({ cls: "ah-tile-spark" });
  renderChart(chartBox, tile.spark);
}
```

- [ ] **Step 8: overview.test.ts anpassen (Set nutzt Keys, Anzeige bleibt deutsch)**

Modify `tests/obsidian/overview.test.ts` — in den beiden Tests, die den `expanded`-Set und `summaryText` prüfen, den Set-Key von `"Körper"`/`"Aktivität"` auf `"body"`/`"activity"` umstellen; die `summaryText(...).startsWith("Körper"/"Aktivität")`-Prüfungen bleiben (Label ist unter globalem `setLang("de")` deutsch).

Test "öffnet genau die Kategorien aus dem gehaltenen Set": ersetze
```ts
    const expanded = new Set<string>(["Körper"]);
```
durch
```ts
    const expanded = new Set<string>(["body"]);
```
(Die `summaryText(d).startsWith("Körper")` / `startsWith("Aktivität")`-Zeilen bleiben unverändert.)

Test "toggle-Event pflegt das Set (auf/zu)": ersetze die beiden
```ts
    expect(expanded.has("Körper")).toBe(true);
    ...
    expect(expanded.has("Körper")).toBe(false);
```
durch
```ts
    expect(expanded.has("body")).toBe(true);
    ...
    expect(expanded.has("body")).toBe(false);
```
(Das `findByTag(...).find((d) => summaryText(d).startsWith("Körper"))` bleibt — es sucht über das deutsche Label.)

- [ ] **Step 9: Alle drei betroffenen Tests + typecheck grün**

Run: `npm test -- tests/core/metric-catalog.test.ts tests/core/view-model.test.ts tests/obsidian/overview.test.ts`
Expected: PASS. Dann `npm run typecheck` → keine Fehler.

- [ ] **Step 10: Commit**

```bash
git add src/core/metric-catalog.ts src/core/view-model.ts src/obsidian/tabs/overview.ts tests/core/metric-catalog.test.ts tests/core/view-model.test.ts tests/obsidian/overview.test.ts
git commit -m "feat(i18n): decouple Category into neutral CategoryKey + localize metrics/stats/overview"
```

---

## Task 3: `workout-catalog` auf `t()` umstellen

**Files:**
- Modify: `src/core/workout-catalog.ts`
- Test: `tests/core/workout-catalog.test.ts`

**Interfaces:**
- Consumes: `t` (`src/vendor/kit/i18n`).
- Produces: `workoutTypeName(type: string): string` (Signatur unverändert), Name via `t("workout."+type)` für bekannte Typen, sonst algorithmischer Fallback.

- [ ] **Step 1: Test auf beide Sprachen umstellen (failing für EN)**

Replace `tests/core/workout-catalog.test.ts`:

```ts
import { workoutTypeName } from "../../src/core/workout-catalog";
import { setLang } from "../../src/vendor/kit/i18n";

describe("workoutTypeName", () => {
  it("kennt kuratierte Workout-Typen je Sprache", () => {
    setLang("de");
    expect(workoutTypeName("HKWorkoutActivityTypeCycling")).toBe("Radfahren");
    expect(workoutTypeName("HKWorkoutActivityTypeTraditionalStrengthTraining")).toBe("Krafttraining");
    setLang("en");
    expect(workoutTypeName("HKWorkoutActivityTypeCycling")).toBe("Cycling");
    expect(workoutTypeName("HKWorkoutActivityTypeTraditionalStrengthTraining")).toBe("Strength Training");
    expect(workoutTypeName("HKWorkoutActivityTypeRunning")).toBe("Running");
  });

  it("Fallback für Unbekannte: Prefix strippen, CamelCase splitten (sprachneutral)", () => {
    setLang("en");
    expect(workoutTypeName("HKWorkoutActivityTypeKickboxing")).toBe("Kickboxing");
    expect(workoutTypeName("HKWorkoutActivityTypeHighIntensityIntervalTraining")).toBe("High Intensity Interval Training");
  });
});
```

- [ ] **Step 2: Test laufen — muss fehlschlagen**

Run: `npm test -- tests/core/workout-catalog.test.ts`
Expected: FAIL (EN liefert noch die deutschen Namen).

- [ ] **Step 3: workout-catalog.ts umstellen**

Replace `src/core/workout-catalog.ts`:

```ts
// Bekannte Apple-Health-Workout-Typen (HKWorkoutActivityType*). Die Anzeigenamen liegen
// sprachabhängig in src/i18n/strings.ts unter "workout.<id>"; unbekannte → Fallback
// (Prefix strippen, CamelCase splitten).
import { t } from "../vendor/kit/i18n";

const KNOWN = new Set<string>([
  "HKWorkoutActivityTypeCardioDance",
  "HKWorkoutActivityTypeCoreTraining",
  "HKWorkoutActivityTypeCrossTraining",
  "HKWorkoutActivityTypeCycling",
  "HKWorkoutActivityTypeElliptical",
  "HKWorkoutActivityTypeFitnessGaming",
  "HKWorkoutActivityTypeFunctionalStrengthTraining",
  "HKWorkoutActivityTypeHiking",
  "HKWorkoutActivityTypeMindAndBody",
  "HKWorkoutActivityTypeOther",
  "HKWorkoutActivityTypePreparationAndRecovery",
  "HKWorkoutActivityTypeRunning",
  "HKWorkoutActivityTypeSwimming",
  "HKWorkoutActivityTypeTableTennis",
  "HKWorkoutActivityTypeTraditionalStrengthTraining",
  "HKWorkoutActivityTypeUnderwaterDiving",
  "HKWorkoutActivityTypeWalking",
  "HKWorkoutActivityTypeYoga",
]);

export function workoutTypeName(type: string): string {
  if (KNOWN.has(type)) return t("workout." + type);
  const stripped = type.replace(/^HKWorkoutActivityType/, "");
  return stripped.replace(/([a-z0-9])([A-Z])/g, "$1 $2").trim() || type;
}
```

- [ ] **Step 4: Test grün + typecheck**

Run: `npm test -- tests/core/workout-catalog.test.ts` → PASS. `npm run typecheck` → keine Fehler.

- [ ] **Step 5: Commit**

```bash
git add src/core/workout-catalog.ts tests/core/workout-catalog.test.ts
git commit -m "feat(i18n): localize workout catalog via t()"
```

---

## Task 4: `RangeKey` sprachneutral (`1J` → `1Y`)

**Files:**
- Modify: `src/core/rollup.ts`
- Modify: `src/obsidian/tabs/detail.ts` (nur die `1J`-Literale; volle Detail-i18n in Task 6)
- Test: `tests/core/rollup.test.ts`

**Interfaces:**
- Produces: `type RangeKey = "1M" | "3M" | "1Y" | "all"`.

- [ ] **Step 1: rollup.test.ts umstellen (failing)**

Modify `tests/core/rollup.test.ts` — alle `"1J"` durch `"1Y"` ersetzen (2 Vorkommen):
```ts
    expect(resolveRange("1Y", dr)).toEqual({ from: "2025-07-18", to: "2026-07-18", granularity: "week" });
```
und
```ts
    expect(resolveRange("1Y", { from: "2000-01-01", to: "2026-03-31" }).from).toBe("2025-03-31");
```
(Beschreibungstexte `"1J → …"` dürfen bleiben oder auf `"1Y"` angepasst werden — kosmetisch.)

- [ ] **Step 2: Test laufen — muss fehlschlagen**

Run: `npm test -- tests/core/rollup.test.ts`
Expected: FAIL (`"1Y"` ist noch kein gültiger RangeKey / kein case).

- [ ] **Step 3: rollup.ts umstellen**

In `src/core/rollup.ts`:
- Zeile 4: `export type RangeKey = "1M" | "3M" | "1Y" | "all";`
- Zeile 21: `case "1Y": return { from: minusMonths(to, 12), to, granularity: "week" };`

- [ ] **Step 4: detail.ts minimal anpassen (kompilierfähig halten)**

In `src/obsidian/tabs/detail.ts`:
- Zeile 8: `const RANGES: RangeKey[] = ["1M", "3M", "1Y", "all"];`
- Zeile 9: `const RANGE_LABEL: Record<RangeKey, string> = { "1M": "1M", "3M": "3M", "1Y": "1J", all: "Alles" };`

(Die vollständige `t()`-Umstellung von `RANGE_LABEL` und den Hinweistexten folgt in Task 6.)

- [ ] **Step 5: Test + typecheck grün**

Run: `npm test -- tests/core/rollup.test.ts` → PASS. `npm run typecheck` → keine Fehler (detail.ts nutzt jetzt konsistent `"1Y"`).

- [ ] **Step 6: Commit**

```bash
git add src/core/rollup.ts src/obsidian/tabs/detail.ts tests/core/rollup.test.ts
git commit -m "refactor(i18n): rename RangeKey 1J -> 1Y (language-neutral identifier)"
```

---

## Task 5: `format.ts` an aktive Sprache koppeln

**Files:**
- Modify: `src/core/format.ts`
- Test: `tests/core/format.test.ts`

**Interfaces:**
- Consumes: `localeTag` (`src/i18n/strings`).
- Produces: `formatValue(n: number, unit: string): string` (Signatur unverändert; Zahlformat folgt `localeTag()`).

- [ ] **Step 1: format.test.ts um EN-Fall erweitern (failing)**

Modify `tests/core/format.test.ts` — im `describe("formatValue", …)` einen EN-Test ergänzen (die bestehenden de-DE-Tests bleiben unter globalem `setLang("de")` grün):

```ts
  it("EN: Tausender-Komma statt Punkt", () => {
    setLang("en");
    expect(formatValue(8432, "count")).toBe("8,432 count");
    expect(formatValue(78.53, "kg")).toBe("78.5 kg");
  });
```

Und den Import am Dateikopf ergänzen:
```ts
import { setLang } from "../../src/vendor/kit/i18n";
```

- [ ] **Step 2: Test laufen — EN-Fall schlägt fehl**

Run: `npm test -- tests/core/format.test.ts`
Expected: FAIL (formatValue nutzt noch hart `"de-DE"` → `"8.432 count"`).

- [ ] **Step 3: format.ts umstellen**

In `src/core/format.ts` — Import ergänzen und `"de-DE"` durch `localeTag()` ersetzen:

```ts
import { localeTag } from "../i18n/strings";

export function formatValue(n: number, unit: string): string {
  const rounded = Math.abs(n) >= 100 ? Math.round(n) : Math.round(n * 10) / 10;
  const num = rounded.toLocaleString(localeTag());
  return unit ? `${num} ${unit}` : num;
}
```
(`formatDuration` bleibt unverändert — sprachneutral.)

- [ ] **Step 4: Test + typecheck grün**

Run: `npm test -- tests/core/format.test.ts` → PASS. `npm run typecheck` → keine Fehler.

- [ ] **Step 5: Commit**

```bash
git add src/core/format.ts tests/core/format.test.ts
git commit -m "feat(i18n): number formatting follows active language locale"
```

---

## Task 6: Restlicher obsidian-Layer + Sprachverdrahtung im onload

**Files:**
- Modify: `src/main.ts` (setLang+registerI18n im onload; Command/Ribbon-Strings)
- Modify: `src/obsidian/dashboard-view.ts` (Tab-Labels, Titel)
- Modify: `src/obsidian/tabs/detail.ts` (RANGE_LABEL→t, Hinweistexte)
- Modify: `src/obsidian/tabs/import.ts` (alle Strings)
- Modify: `src/obsidian/tabs/workouts.ts` (Überschriften, Empty)
- Modify: `src/obsidian/health-source.ts` (Fehlermeldung)
- Modify: `tests/__mocks__/obsidian.ts` (`getLanguage`-Export für main-host-Test)
- Test: `tests/obsidian/detail.test.ts` (Setup bleibt via globalem de; kleine Anpassung falls nötig)

**Interfaces:**
- Consumes: `t`, `pickLang`, `setLang` (`src/vendor/kit/i18n`), `registerI18n` (`src/i18n/strings`), `getLanguage` (obsidian).

- [ ] **Step 1: obsidian-Mock um getLanguage ergänzen**

In `tests/__mocks__/obsidian.ts` einen Export ergänzen (Rückgabe egal, Tests setzen die Sprache über das globale Setup):
```ts
export function getLanguage(): string { return "en"; }
```
(Falls die Datei bereits ein `getLanguage` hat: überspringen.)

- [ ] **Step 2: main.ts — Sprachdetektion im onload + Command/Ribbon lokalisieren**

In `src/main.ts`:
- Import ergänzen: `getLanguage` aus `"obsidian"`; `pickLang, setLang` aus `"./vendor/kit/i18n"`; `t, registerI18n` aus `"./i18n/strings"`.
- Am Anfang von `onload()`, **vor** `registerView`/`addCommand`/`addRibbonIcon`:
```ts
    setLang(pickLang(safeGetLanguage()));
    registerI18n();
```
- Command-Name (Zeile 23): `name: t("cmd.openDashboard"),`
- Ribbon (Zeile 26): `this.addRibbonIcon("heart-pulse", t("ribbon.tooltip"), () => { void this.activateView(); });`
- Modulnahe Helper-Funktion (außerhalb der Klasse) ergänzen:
```ts
function safeGetLanguage(): string | null {
  try { return getLanguage(); } catch { return null; }
}
```

- [ ] **Step 3: dashboard-view.ts — Tabs + Titel lokalisieren**

In `src/obsidian/dashboard-view.ts`:
- Import ergänzen: `import { t } from "../vendor/kit/i18n";`
- Das `TABS`-Array trägt heute feste Labels. Da die Sprache beim `onload` feststeht, Labels zur Renderzeit auflösen: `label`-Feld aus dem Array entfernen und stattdessen über einen Key mappen. Ersetze das Array (Zeilen 21–25) durch:
```ts
const TABS: Array<{ id: TabId; labelKey: string; icon: string }> = [
  { id: "overview", labelKey: "tab.overview", icon: "layout-grid" },
  { id: "detail", labelKey: "tab.detail", icon: "line-chart" },
  { id: "workouts", labelKey: "tab.workouts", icon: "dumbbell" },
];
```
- In `renderRoot()` die zwei Verwendungen von `t.label` auf `t(tab.labelKey)` umstellen. Konkret (Zeilen 86, 89), wobei die Schleifenvariable `t` heißt — zur Vermeidung der Namenskollision mit der `t()`-Funktion die Schleifenvariable umbenennen:
```ts
    for (const tab of TABS) {
      const btn = head.createDiv({ cls: "ah-tab" });
      btn.setAttribute("role", "tab");
      btn.setAttribute("aria-label", t(tab.labelKey));
      const icon = btn.createSpan({ cls: "ah-tab-icon" });
      setIcon(icon, tab.icon);
      btn.createSpan({ cls: "ah-tab-label", text: t(tab.labelKey) });
      btn.addEventListener("click", () => { this.switchTab(tab.id); this.renderActive(); });
      this.tabButtons.set(tab.id, btn);
    }
```
- `getDisplayText()` (Zeile 47): `return t("view.title");`
- **Wichtig:** Die zweite `TABS`-Schleife in `renderRoot()` (Panels anlegen, Zeilen 95–98) nutzt ebenfalls `t` als Schleifenvariable — dort in `tab` umbenennen, um konsistent zu bleiben:
```ts
    for (const tab of TABS) {
      const panel = content.createDiv({ cls: "ah-panel" });
      this.panels.set(tab.id, panel);
    }
```

- [ ] **Step 4: detail.ts — RANGE_LABEL + Hinweistexte auf t()**

In `src/obsidian/tabs/detail.ts`:
- Import ergänzen: `import { t } from "../../vendor/kit/i18n";`
- `RANGE_LABEL`-Konstante (Zeile 9) **entfernen**.
- Zeile 17: `hint.createSpan({ text: t("detail.pickMetric") });`
- Zeile 28: `const btn = tabs.createEl("button", { text: t("range." + rk) });`
- Zeile 36: `hint.createSpan({ text: t("detail.noData") });`

- [ ] **Step 5: import.ts — alle Strings auf t()**

Replace `src/obsidian/tabs/import.ts`:

```ts
import { ButtonComponent } from "obsidian";
import type { ImportPhase, ImportState } from "../../core/import-state";
import { t } from "../../vendor/kit/i18n";

export interface ImportActions {
  choose(): void;
  abort(): void;
}

const PHASE_KEY: Record<ImportPhase, string> = {
  unzipping: "import.phase.unzipping",
  parsing: "import.phase.parsing",
  writing: "import.phase.writing",
};

/** Rendert den Import-Screen für den gegebenen Zustand. Ersetzt den Inhalt von `el`. */
export function renderImport(el: HTMLElement, state: ImportState, actions: ImportActions): void {
  el.empty();
  const box = el.createDiv({ cls: "ah-empty ah-import" });

  if (state.status === "running") {
    box.createEl("h3", { text: t("import.running.title") });
    box.createEl("p", { cls: "ah-import-file", text: state.fileName });
    box.createEl("p", { cls: "ah-import-phase", text: t(PHASE_KEY[state.phase]) });
    box.createEl("p", {
      cls: "ah-import-count",
      text: state.records > 0 ? t("import.records", state.records.toLocaleString(localeTag())) : "…",
    });
    if (state.phase === "unzipping" || state.phase === "parsing") {
      new ButtonComponent(box).setButtonText(t("import.cancel")).onClick(() => { actions.abort(); });
    }
    return;
  }

  if (state.status === "failed") {
    box.createEl("h3", { text: t("import.failed.title") });
    box.createEl("p", { cls: "ah-import-error", text: state.message });
    new ButtonComponent(box).setButtonText(t("import.retry")).setCta()
      .onClick(() => { actions.choose(); });
    return;
  }

  if (state.status === "aborted") {
    box.createEl("h3", { text: t("import.aborted.title") });
    box.createEl("p", { text: t("import.aborted.body") });
    new ButtonComponent(box).setButtonText(t("import.choose")).setCta()
      .onClick(() => { actions.choose(); });
    return;
  }

  // idle / done-ohne-Cache
  box.createEl("h3", { text: t("import.idle.title") });
  box.createEl("p", { text: t("import.idle.body") });
  new ButtonComponent(box).setButtonText(t("import.choose")).setCta()
    .onClick(() => { actions.choose(); });
}
```

**Hinweis:** `import.records` erwartet die bereits formatierte Zahl als `{0}`; deshalb wird `state.records.toLocaleString(localeTag())` als Argument übergeben. Import für `localeTag` am Kopf ergänzen:
```ts
import { localeTag } from "../../i18n/strings";
```

- [ ] **Step 6: workouts.ts — Überschriften + Empty auf t()**

In `src/obsidian/tabs/workouts.ts`:
- Import ergänzen: `import { t } from "../../vendor/kit/i18n";`
- Zeile 16: `el.createDiv({ cls: "ah-detail-hint", text: t("workouts.emptyExport") });`
- Zeile 20: `el.createEl("h3", { text: t("workouts.perMonth") });`
- Zeile 25: `el.createEl("h3", { text: t("workouts.recent") });`

- [ ] **Step 7: health-source.ts — Fehlermeldung auf t()**

In `src/obsidian/health-source.ts`:
- Import ergänzen: `import { t } from "../vendor/kit/i18n";`
- Die Zeile `if (!matched) throw new Error("Export.xml nicht im Zip gefunden");` → `if (!matched) throw new Error(t("error.exportNotFound"));`

- [ ] **Step 8: Volllauf aller Tests + Lint + Typecheck**

Run: `npm test`
Expected: PASS (alle Suites). detail.test.ts bleibt grün (globales `setLang("de")`: „Schritte", „Summe", „1M" unverändert vorhanden).
Run: `npm run typecheck` → keine Fehler.
Run: `npm run lint` → 0 errors.

- [ ] **Step 9: Store-Scanner-Muster prüfen (LESSON)**

Run: `grep -rn "document.createElement\|activeWindow" src/`
Expected: keine Treffer (i18n führt keine solchen Muster ein). Falls doch → fixen.

- [ ] **Step 10: Commit**

```bash
git add src/main.ts src/obsidian/dashboard-view.ts src/obsidian/tabs/detail.ts src/obsidian/tabs/import.ts src/obsidian/tabs/workouts.ts src/obsidian/health-source.ts tests/__mocks__/obsidian.ts
git commit -m "feat(i18n): localize obsidian layer + wire language detection in onload"
```

---

## Abschluss (nach Task 6, außerhalb der Task-Schleife)

- [ ] **Manueller Smoke-Test** (obsidian-Layer-Bugs sind in Unit-Tests unsichtbar — Cockpit-LESSON): Plugin bauen (`npm run build`), in ProtoVault deployen, Dashboard bei Obsidian-Sprache Deutsch UND Englisch öffnen; Import-Screen, Übersicht (Kategorien de/en), Detail (Range-Buttons, Stats), Workouts prüfen. Kategorie-Aufklappzustand muss Sprachwechsel-unabhängig über Re-Render stabil bleiben.
- [ ] **README:** kurzen Hinweis ergänzen, dass die Sprache Obsidians UI-Sprache folgt (DE/EN).
- [ ] **REGISTRY** (`../REGISTRY.md`, i18n-Abschnitt): apple-health als Consumer (vendored `src/vendor/kit/i18n.ts`) eintragen; `detectLang`-Zeile präzisieren (Pre-1.8.0-Artefakt, `pickLang(getLanguage())` ist der Nachfolger; keine `detectLang`-Promotion getriggert) — siehe Spec „Abschluss-Nachtrag".
- [ ] **Cockpit** (`$VAULT/25_Coding/apple-health/apple-health.md`): Slice 3b als erledigt vermerken, Fokus auf Slice 3c setzen.
</content>
